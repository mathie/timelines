<% content_for :title, "Edit #{@category.title}" %>

<%= render 'shared/errors', model: @category %>

<%= bootstrap_form_for @category do |f| %>
  <%= f.text_field :title, autofocus: true %>

  <%= f.submit %>
<% end %>